#include <stdio.h>
#include <stdlib.h>

void update(int *a, int *b)
{
    int x = *a;
    int y = *b;

    *a = x + y;
    *b = abs(x - y);
}

int main()
{
    int a, b;

    scanf("%d", &a);
    scanf("%d", &b);

    update(&a, &b);

    printf("%d\n", a);
    printf("%d\n", b);

    return 0;
}
